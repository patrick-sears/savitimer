

This directory is for files a user can add as alternates to
the default config and data files.

Only the README.txt will be tracked by git.

Currently, savitime.config is the only file usable in the user dir.





